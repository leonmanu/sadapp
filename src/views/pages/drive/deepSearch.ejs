<!DOCTYPE html>
<html lang="es" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Búsqueda Avanzada de PDFs</title>
    <!-- Corregido el atributo integrity -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Corregido el atributo integrity -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" xintegrity="sha512-SnH5WK+bZxgPHs44uWIX+LLMDJdZfR72Ua1kR0p83gXkXQ8+D6C7S6A8l9A/0+Cg6E1/w0n9+g6F8/lQz7rL5Vw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        .custom-container {
            max-width: 1000px;
            margin: auto;
        }
        body {
            padding-top: 70px; 
        }
        .file-icon {
            font-size: 1.25em;
            margin-right: 8px;
        }
        .card-header, .search-form-card {
            background-color: #343a40; /* bg-secondary dark */
        }
        .search-form-card {
            border: 1px solid #495057;
        }
    </style>
</head>
<body class="bg-dark text-light">

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
        <div class="container-fluid custom-container">
            <a class="navbar-brand text-primary fw-bold" href="/drive/search"><i class="fas fa-google-drive me-2"></i> Drive Search</a>
            <span class="navbar-text text-light">
                <i class="fas fa-file-pdf me-2"></i> Búsqueda de Contenido en PDF
            </span>
        </div>
    </nav>

    <div class="container my-4 p-4 rounded shadow custom-container bg-body-tertiary">
        <h1 class="h3 mb-4 text-primary"><i class="fas fa-filter me-2"></i> Buscar Palabras dentro de Archivos PDF</h1>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger" role="alert">
                <i class="fas fa-exclamation-triangle me-2"></i> <%= error %>
            </div>
        <% } %>

        <!-- Tarjeta del Formulario de Búsqueda Simplificada -->
        <div class="card search-form-card mb-4">
            <div class="card-header fw-bold text-light">
                Filtro Único: Contenido de Texto
            </div>
            <div class="card-body">
                <form action="/drive/search" method="GET" class="row g-3">
                    
                    <!-- Campo oculto para forzar la búsqueda solo en PDFs -->
                    <input type="hidden" name="fileType" value="pdf"> 
                    
                    <!-- 1. Palabra Clave -->
                    <div class="col-12">
                        <label for="q" class="form-label text-warning"><i class="fas fa-keyboard me-1"></i> Texto o Palabra Clave</label>
                        <input type="text" name="q" id="q" 
                               value="<%= typeof q !== 'undefined' ? q : '' %>" 
                               class="form-control form-control-lg" placeholder="Ej: Número de factura, Nombre del proyecto, Informe 2025" required>
                        <small class="form-text text-muted">La búsqueda se limitará a archivos de tipo PDF.</small>
                    </div>

                    <!-- Botón de Búsqueda -->
                    <div class="col-12 text-center mt-4">
                        <button type="submit" class="btn btn-primary btn-lg px-5 shadow-lg">
                            <i class="fas fa-search me-2"></i> Buscar PDFs
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Resultados de la Búsqueda -->
        <% if (typeof files !== 'undefined' && files.length > 0) { %>
            <h2 class="h5 mt-5 mb-3 text-info"><i class="fas fa-list-ul me-2"></i> Resultados Encontrados (<%= files.length %>)</h2>
            <div class="table-responsive">
                <table class="table table-dark table-striped table-hover align-middle">
                    <thead>
                        <tr>
                            <th scope="col" style="width: 60%;">Nombre</th>
                            <th scope="col" style="width: 20%;">Tipo</th>
                            <th scope="col" style="width: 20%;">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% files.forEach(function(file) { %>
                            <tr>
                                <td><%= file.name %></td>
                                <td>
                                    <!-- Dado que filtramos por PDF, solo mostramos el ícono de PDF -->
                                    <i class="fas fa-file-pdf text-danger file-icon" title="<%= file.mimeType %>"></i> 
                                    <span class="text-secondary small d-none d-sm-inline">PDF</span>
                                </td>
                                <td>
                                    <% if (file.webViewLink) { %>
                                        <a href="<%= file.webViewLink %>" target="_blank" class="btn btn-sm btn-outline-info" title="Abrir en Google Drive">
                                            <i class="fas fa-external-link-alt"></i> Ver
                                        </a>
                                    <% } else { %>
                                        <span class="text-muted small">No link</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else if (typeof files !== 'undefined' && files.length === 0 && typeof q !== 'undefined' && q) { %>
            <div class="alert alert-warning mt-4" role="alert">
                No se encontraron archivos PDF que coincidan con la palabra clave: **<%= q %>**.
            </div>
        <% } %>

        <div class="text-center mt-5">
            <!-- NOTA: Si necesitas volver a la búsqueda general (sin filtros) utiliza /drive/search -->
            <a href="/drive/search" class="btn btn-outline-light"><i class="fas fa-arrow-left me-2"></i> Volver a Búsqueda General</a>
        </div>
    </div>
    
    <!-- Corregido el atributo integrity -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" xintegrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
